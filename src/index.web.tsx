import { AppRegistry } from 'react-native'
import App from './App'

AppRegistry.registerComponent('DeepRead', () => App)
AppRegistry.runApplication('DeepRead', { initialProps: {}, rootTag: document.getElementById('root') })
